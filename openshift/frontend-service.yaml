apiVersion: v1
kind: Service
metadata:
  name: openshift-dashboard-frontend
  labels:
    app: openshift-dashboard
    component: frontend
spec:
  ports:
  - port: 8080
    targetPort: 8080
    protocol: TCP
    name: http
  selector:
    app: openshift-dashboard
    component: frontend
  type: ClusterIP

